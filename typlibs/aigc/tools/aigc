#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
repo_dir="$(cd -- "$script_dir/../../.." && pwd)"
export UV_CACHE_DIR="${UV_CACHE_DIR:-$repo_dir/target/cache/uv}"
exec mise x -- uv run --script "$script_dir/aigc.py" "$@"
