# slider-h

`slider-h` is a theme for `slider`.

## Dependencies

This theme imports `@preview/shadowed:0.3.0` to render soft, close elevation shadows for non-`plain` paper materials. The dependency is a theme implementation detail; `slider` core does not depend on it.

Typst downloads Universe packages on first compile and then uses the local cache.
